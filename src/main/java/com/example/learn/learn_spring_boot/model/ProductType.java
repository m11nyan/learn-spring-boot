package com.example.learn.learn_spring_boot.model;

public enum ProductType {
    // Enum for product types
    // This enum is used to categorize products in the application
    SMARTPHONE, TABLET, LAPTOP
}
